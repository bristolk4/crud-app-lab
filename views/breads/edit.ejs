<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Edit <%= bread.name %></title>
  </head>
  <body>
    <h1>Edit <%= bread.name %></h1>
    <form action="/breads/<%=bread._id%>?_method=PUT" method="POST">
      <label for="name">Name:</label>
      <input type="text" name="name" value="<%= bread.name %>" />
      <div class="checkbox-div">
      <label for="isProofed">Is it proofed?</label>
      <input type="checkbox" name="isProofed" <% if (bread.isProofed) { %>checked<% } %> >
      </div>
      <button type="submit">Update Bread</button>
    </form>
    <a class="breads-index-link" href="/breads">
        <div class="index-link-text">Back to Breads</a></div>
    <a class="home-link" href="/">
        <div class="home-link-text">Home Page</a></div>
  </body>
  