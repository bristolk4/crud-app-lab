<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= bread.name %></title> <!-- Dynamic title based on bread name -->
  </head>
  <body>
    <h1><%= bread.name %></h1>
    <% if (bread.isProofed) { %>
      <p>This dough is proofed!</p>
    <% } else { %>
      <p>This dough is not proofed!</p>
    <% } %>
    <form action="/breads/<%=bread._id%>?_method=DELETE" method="POST">
        <button type="submit">Delete <%= bread.name %></button>
      </form>
      <a href="/breads/<%= bread._id %>/edit">Edit <%= bread.name %></a>
    <a href="/breads/">Back to Breads</a>

  </body>
</html>